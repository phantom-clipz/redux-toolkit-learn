import React from 'react';
import { Counter } from '@/components/Common/Counter/Counter';

const Page = () => {
   return (
      <div>
         <Counter />
      </div>
   );
};

export default Page;